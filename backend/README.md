
To run backend:

Create virtual environment by running
1.  python -m venv server-venv
2. (on Mac/Linux) source server-venv/bin/activate
   (on Windows) server-venv\Scripts\activate
3. pip install -r backend/requirments.txt

Then run the application:
1. python backend/app.py
